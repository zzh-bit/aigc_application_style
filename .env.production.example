# 复制为 .env.production 后再执行 npm run build:android / npm run rebuild
# 真机与 APK 必须能访问该地址；不要用 http://10.0.2.2（仅 Android 模拟器指向本机）。
#
# 百度云阶段 A（HTTP + IP）示例：
# NEXT_PUBLIC_API_BASE_URL=http://154.12.28.62:3000
#
# 阶段 B / 当前验证脚本默认域名：
NEXT_PUBLIC_API_BASE_URL=https://api.wdzsyyh.cloud
